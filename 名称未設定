import requests
import threading
# -*- coding: utf-8 -*-
from joblib import Parallel, delayed
from time import time

def posting() :
    requests.post('http://jshaskellcalc.herokuapp.com/')
    time.sleep(0.05)

r = Parallel(n_jobs=-1)( [delayed(posting)(i) for i in range(10000)] )
